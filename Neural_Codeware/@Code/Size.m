function iSize = Size(this)
    %---------------------------------------------------------------------------
    % Usage:
    %    iSize = this.Size()
    % Description:
    %    Return the number of codewords in this code.
    %---------------------------------------------------------------------------
    
    iSize = Size(this.Words);
end