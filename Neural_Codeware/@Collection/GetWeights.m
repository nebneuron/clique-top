function cvWeights = GetWeights(this)
    %--------------------------------------------------------------------------------
    % Usage:
    %    cvWeights = this.GetWeights()
    % Description:
    %    Return the column vector of weights that have been assigned to
    %    the elements of this collection.
    %--------------------------------------------------------------------------------
    
    cvWeights = this.Weights;
end